import { TestflatDirective } from './testflat.directive';

describe('TestflatDirective', () => {
  it('should create an instance', () => {
    const directive = new TestflatDirective();
    expect(directive).toBeTruthy();
  });
});
